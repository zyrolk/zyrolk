import { RefreshCw } from 'lucide-react';
import type { SupplierSyncSummary } from '../../types/supplier';

interface SupplierSyncCardProps { readonly sync: SupplierSyncSummary }

export function SupplierSyncCard({ sync }: SupplierSyncCardProps) {
  return <article className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5" aria-labelledby="supplier-sync-title"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-500/10 text-blue-400"><RefreshCw className="h-5 w-5" aria-hidden="true" /></div><div><h3 id="supplier-sync-title" className="text-base font-black text-white">Sync history summary</h3><p className="text-[11px] text-slate-500">Valid dated records only</p></div></div>{sync.totalSyncs === 0 ? <p className="mt-5 rounded-xl border border-dashed border-slate-700 p-4 text-xs text-slate-500">Unavailable — no valid sync history exists.</p> : <><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-950/50 p-3"><p className="text-[10px] uppercase tracking-wider text-slate-500">Success rate</p><p className="mt-1 text-xl font-black text-white">{sync.successRate === null ? 'Unavailable' : `${sync.successRate.toFixed(1)}%`}</p></div><div className="rounded-xl bg-slate-950/50 p-3"><p className="text-[10px] uppercase tracking-wider text-slate-500">Runs</p><p className="mt-1 text-xl font-black text-white">{sync.totalSyncs}</p></div></div><div className="mt-4 flex flex-wrap gap-3 text-[11px] text-slate-400"><span>{sync.successfulSyncs} successful</span><span>{sync.failedSyncs} failed</span><span>{sync.unknownSyncs} unknown</span></div><div className="mt-5 border-t border-slate-800 pt-4"><p className="text-[10px] font-black uppercase tracking-wider text-slate-500">Last successful sync</p>{sync.lastSuccessfulSync ? <><p className="mt-2 text-sm font-black text-white">{sync.lastSuccessfulSync.relative}</p><time className="mt-1 block text-xs text-slate-500" dateTime={sync.lastSuccessfulSync.iso}>{sync.lastSuccessfulSync.absolute}</time></> : <p className="mt-2 text-xs text-slate-500">Unavailable — no successful sync is recorded.</p>}</div></>}</article>;
}
